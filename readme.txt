ffsearch 

search
-t	specify the table to search in.
-c	specify the column to search in.
-s	specify the search term.
-l	specify the limit.
-f	specify if the search is fuzzy.
-a	specify the search to be an AND query.

index
-f	specify the csv file path to index.
-c	specify the columns to index.